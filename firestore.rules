rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // App data is accessed through the server Admin SDK, which bypasses rules.
    // Keep client Firestore locked down unless direct browser reads/writes are added intentionally.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
