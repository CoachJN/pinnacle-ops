# Modules

Feature modules own domain rules, validation, repository contracts, server orchestration, and module-specific UI for one business area.

Shared code remains in `src/lib`, request/server boundaries remain in `src/server`, and reusable cross-module types remain in `src/types`.

